{
  "name": "@procezly/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "scripts": {
    "dev": "wrangler dev --port 8787",
    "build": "wrangler deploy --dry-run --outdir dist"
  },
  "dependencies": {
    "hono": "^4.4.7",
    "jose": "^5.6.3",
    "@procezly/shared": "0.0.1",
    "@procezly/db": "0.0.1"
  },
  "devDependencies": {
    "wrangler": "^3.78.10"
  }
}
